import InputNodeParams from '../../../interfaces/InputNodeParams';

const LoginInputNodesData: Record<string, InputNodeParams> = {
  loginInput: {
    tag: 'input',
    css: ['login-input'],
    type: 'text',
  },
};

export default LoginInputNodesData;
